import {Construct} from 'constructs';

export class Backend extends Construct {
  constructor(parent: Construct, id: string) {
    super(parent, id);

    // other integration with AWS and existing services/EventBuses
  }
}
